# Agents as a Service

Landing page for selling pre-configured AI agents by monthly subscription — priced in rands.

Built with Next.js 16, Tailwind CSS, shadcn/ui.

```bash
npm run dev    # http://localhost:3000
npm run build  # static export
```
